# Generate this in QuickFile:
# Account Settings > Third Party Integration > API
QUICKFILE_API_TOKEN=replace-with-your-personal-bearer-token

# Optional. Defaults are shown.
# QUICKFILE_BASE_URL=https://api-beta.quickfile.co.uk
# QUICKFILE_TIMEOUT_MS=30000
# QUICKFILE_MAX_RESPONSE_BYTES=2000000
